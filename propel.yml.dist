propel:
    paths:
        # The directory where Propel expects to find your `schema.xml` file.
        schemaDir: src

        # The directory where Propel should output generated object model classes.
        phpDir: src

    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=db;port=3306;dbname=kantaria
                user: kantaria
                password: kantaria
                settings:
                    charset: utf8
